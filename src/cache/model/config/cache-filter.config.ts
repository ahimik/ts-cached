import { CacheFilter } from '../cache-filter';

/**
 * Filter configuration.
 */
export interface CacheFilterConfig {
    filter?: CacheFilter; // cache filtering function based on provided arguments
}
